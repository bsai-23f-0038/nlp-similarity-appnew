# ──────────────────────────────────────────────────────────────
# requirements.txt
# NLP Semantic Similarity Explorer
# Compatible with Streamlit Community Cloud & local environments
# ──────────────────────────────────────────────────────────────

# Core web framework
streamlit>=1.32.0,<2.0.0

# NLP model & sentence embeddings (free, Apache 2.0)
sentence-transformers>=2.7.0,<3.0.0

# PyTorch CPU backend (community cloud uses CPU; GPU is auto-detected locally)
torch>=2.2.0
torchvision>=0.17.0

# HuggingFace model hub
transformers>=4.40.0,<5.0.0
huggingface-hub>=0.22.0

# Dimensionality reduction (PCA)
scikit-learn>=1.4.0,<2.0.0

# Interactive charts
plotly>=5.20.0,<6.0.0

# Numerical & data
numpy>=1.26.0,<2.0.0
pandas>=2.2.0,<3.0.0

# Tokenizer backend
tokenizers>=0.19.0

# Sentence-transformers dependencies
scipy>=1.12.0
Pillow>=10.2.0
tqdm>=4.66.0
